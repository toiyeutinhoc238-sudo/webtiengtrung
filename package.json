{
  "name": "hsk-flashcards-fullstack",
  "version": "1.0.0",
  "description": "Full-stack HSK vocabulary flashcard study web application",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "keywords": [
    "hsk",
    "chinese",
    "flashcard",
    "fullstack"
  ],
  "author": "Antigravity",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
